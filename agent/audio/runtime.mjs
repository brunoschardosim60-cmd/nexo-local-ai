export function createAudioRuntime({ provider, artifacts, enabled = false }) {
  const metrics=[];
  return {
    async transcribe(input, options={}) { if (!enabled) throw new Error('STT de servidor não configurado; o reconhecimento do navegador é apenas fallback, não STT V2.'); const started=performance.now();const result=await provider.transcribe(input,{...options,timestamps:options.timestamps!==false,languageDetection:options.languageDetection!==false});metrics.push({kind:'stt',durationMs:performance.now()-started,at:new Date().toISOString()});return result; },
    async synthesize(input, options={}) { if (!enabled) throw new Error('TTS de servidor não configurado; a voz do navegador é apenas fallback, não TTS V2.'); const started=performance.now();const personality={pace:input.pace||1,energy:input.energy||'calm',pauses:input.pauses||'natural',emphasis:input.emphasis||'selective'};const output = await provider.synthesize({...input,voice:input.voice||'nexo-pt-BR',speed:personality.pace},options); const artifact = await artifacts.saveBuffer({ type: 'audio', mimeType: output.mimeType, provider: output.provider, buffer: output.buffer, metadata: { voice: input.voice || 'nexo-pt-BR', textChars: input.text.length,personality,streaming:Boolean(output.streaming) } });metrics.push({kind:'tts',durationMs:performance.now()-started,firstAudioMs:output.firstAudioMs||null,at:new Date().toISOString()});return { artifact,personality,streaming:Boolean(output.streaming) }; },
    metrics:()=>metrics.slice(-100),health: () => ({ enabled,version:'2.0.0',identity:'Nexo',maturity:enabled?'BETA':'UNAVAILABLE',stt:{streaming:'provider-dependent',timestamps:true,languageDetection:true,speakers:'provider-dependent'},tts:{streaming:'provider-dependent',voiceIdentity:true},bargeIn:true,fullDuplex:'provider-dependent',provider:'configurable-local-http',browserFallback:'Web Speech API (não equivale a realtime speech)' }), availability: () => provider.health(),
  };
}
