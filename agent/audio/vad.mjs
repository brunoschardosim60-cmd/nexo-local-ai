export function createVoiceActivityDetector({threshold=0.018,silenceMs=700,minSpeechMs=160}={}){let speaking=false;let speechStartedAt=null;let lastActiveAt=null;function process({rms,at=Date.now()}){const active=Number(rms)>=threshold;const events=[];if(active){lastActiveAt=at;if(!speaking){speaking=true;speechStartedAt=at;events.push({type:'speech-start',at});}}else if(speaking&&lastActiveAt!==null&&at-lastActiveAt>=silenceMs){const duration=at-speechStartedAt;speaking=false;if(duration>=minSpeechMs)events.push({type:'speech-end',at,durationMs:duration});speechStartedAt=null;}return{speaking,events};}return{process,reset(){speaking=false;speechStartedAt=null;lastActiveAt=null;},health:()=>({threshold,silenceMs,minSpeechMs,streaming:true})};}
