export function createMediaProviderRegistry(){const providers=new Map();function register(provider){if(!provider?.id)throw new Error('Provider multimodal sem id.');providers.set(provider.id,provider);return provider;}function list(){return[...providers.values()].map(item=>({id:item.id,label:item.label||item.id,local:item.local!==false,capabilities:item.capabilities||[],maturity:item.maturity||'EXPERIMENTAL',cost:item.cost||'free-local'}));}function select(capability){return list().filter(item=>item.capabilities.includes(capability)).sort((a,b)=>Number(b.local)-Number(a.local)||['STABLE','BETA','EXPERIMENTAL','UNAVAILABLE'].indexOf(a.maturity)-['STABLE','BETA','EXPERIMENTAL','UNAVAILABLE'].indexOf(b.maturity))[0]||null;}return{register,list,select,health:()=>({dynamic:true,providers:list(),capabilities:[...new Set(list().flatMap(item=>item.capabilities))]})};}
