const PATTERNS={WORK:/\b(trabalho|cliente|prazo|relatorio|entrega|reuniao|profissional|execut|corrig|valid|teste|projeto)\w*/i,CREATIVE:/\b(criativ|brainstorm|ideia|variac|explor|historia|design|nome|roteiro)\w*/i,STUDY:/\b(estud|aprend|prova|revis|conceito|exercicio|quiz|tutor|ensine)\w*/i,FOCUS:/\b(foco|sem interrupcao|nao me interrompa|concentr)\w*/i};
const INSTRUCTIONS={WORK:'Modo trabalho: priorize precisão, execução, evidência e brevidade.',CREATIVE:'Modo criativo: explore variações, critique alternativas e itere sem apresentar hipótese como fato.',STUDY:'Modo aprendizagem: verifique domínio, use recuperação ativa e trate erros como sinais de conceitos subjacentes.',FOCUS:'Modo foco: minimize interrupções, sugestões laterais e texto não essencial.',GENERAL:''};
export function inferPersonalMode({question='',override=null,settings={}}={}){if(override&&INSTRUCTIONS[override])return{mode:override,confidence:1,source:'override',instruction:INSTRUCTIONS[override]};if(settings.focusMode)return{mode:'FOCUS',confidence:1,source:'setting',instruction:INSTRUCTIONS.FOCUS};const normalized=String(question).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const matches=Object.entries(PATTERNS).filter(([,pattern])=>pattern.test(normalized)).map(([mode])=>mode);const mode=matches.length===1?matches[0]:'GENERAL';return{mode,confidence:matches.length===1?0.88:0.45,source:matches.length===1?'inferred':'default',instruction:INSTRUCTIONS[mode]};}
