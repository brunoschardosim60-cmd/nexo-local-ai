import { defineConnector } from './contracts.mjs';
export function createConnectorManager({registry,vault}){const connectors=new Map();function register(raw){const connector=defineConnector(raw);connectors.set(connector.id,connector);registry.register(connector);return connector;}function disconnect(id){const connector=connectors.get(id);if(!connector)throw new Error('Connector não encontrado.');for(const ref of connector.secretRefs||[])vault.revoke(ref);registry.setEnabled(id,false);return{id,disconnected:true};}function list(){return[...connectors.values()].map(connector=>{const item={...connector};delete item.execute;return item;});}return{register,disconnect,list,health:()=>({configured:connectors.size,leastPrivilege:true,revocable:true,secretHandles:true})};}
