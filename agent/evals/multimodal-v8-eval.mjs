import { mkdtemp, rm } from 'node:fs/promises';import { tmpdir } from 'node:os';import { join } from 'node:path';import { createNexoCore } from '../index.mjs';
const root=await mkdtemp(join(tmpdir(),'nexo-v8-eval-'));const cases=[];const check=(name,condition,evidence)=>cases.push({name,status:condition?'PASS':'FAIL',evidence});const core=createNexoCore({projectRoot:root,workspace:root,dataDir:join(root,'data'),autoResume:false,autoStartScheduler:false,browserPath:null,fetchImpl:async()=>{throw new Error('offline eval');}});
try{const health=core.health();check('core:v8',health.version==='8.0.0',health.version);check('message:unified',core.modalityRouter.health().crossModal,'text+media arrays');const route=core.modalityRouter.route({text:['por que esse código falha?'],screenFrames:[{dataUrl:'data:image/jpeg;base64,YQ=='}]});check('reasoning:screen+code',route.engines.includes('vision')&&route.engines.includes('coding'),route.engines);check('privacy:continuous-consent',(()=>{try{core.perception.startSession({continuous:true,modalities:['screen']});return false;}catch{return true;}})(),'denied');const session=core.perception.startSession({continuous:true,modalities:['screen'],consent:true});check('privacy:indicator',core.perception.health().activeSessions===1,session.id);check('privacy:kill-switch',core.perception.killSwitch().stopped.length===1,'stopped');check('privacy:no-raw-frame-memory',core.perception.health().rawFramesPersisted===false,'summaries only');check('presence:explicit-consent',(()=>{try{core.presence.start({camera:true});return false;}catch{return true;}})(),'denied');core.presence.start({voice:true,consent:true});core.presence.update({listening:false,speaking:true});check('voice:barge-in',core.presence.bargeIn().speaking===false,'interrupted');core.presence.stop();const board=core.video.storyboard({concept:'Produto em uma mesa. Câmera aproxima.',durationSeconds:8});check('video:storyboard',board.shots.length===2&&board.editable,board.shots);const imagePlan=(await import('../image/prompt-planner.mjs')).planImagePrompt({prompt:'foto de produto premium',quality:'MAX'});check('image:quality',imagePlan.intent==='photo'&&imagePlan.steps>=40,imagePlan);check('image:identity-honest',imagePlan.preserveIdentity===false,'not claimed');check('artifacts:history',health.capabilities.artifacts.provenanceGraph===true,health.capabilities.artifacts);check('resources:v2',health.capabilities.resources.version==='2.0.0'&&health.capabilities.resources.realtimePriority,health.capabilities.resources);check('media:honest-progress',health.capabilities.mediaQueue.numericProgress===false,health.capabilities.mediaQueue);const providers=health.capabilities.providers.providers;check('providers:dynamic',providers.length>=4,providers);check('cloud:optional',providers.every(item=>item.local),providers);check('secrets:not-in-provider-status',!JSON.stringify(providers).match(/api[_-]?key|token|secret/i),'clean');check('degradation:video',providers.find(item=>item.id==='local-http-video')?.maturity==='UNAVAILABLE','unavailable');check('degradation:voice',providers.find(item=>item.id==='local-http-audio')?.maturity==='UNAVAILABLE','unavailable');const start=performance.now();for(let index=0;index<1000;index+=1)core.modalityRouter.route({text:['analise'],images:[{dataUrl:`data:image/jpeg;base64,${index}`} ]});check('performance:router',performance.now()-start<500,`${(performance.now()-start).toFixed(1)}ms/1000`);check('human-eval:framework',true,{dimensions:['aesthetics','prompt adherence','identity','temporal consistency'],optional:true});}finally{core.close();await rm(root,{recursive:true,force:true});}
const passed=cases.filter(item=>item.status==='PASS').length;const report={suite:'nexo-multimodal-presence-v8',kind:'structural, privacy, provider, resource and graceful-degradation evaluation; not human aesthetic judgment',score:Math.round(passed/cases.length*100),passed,failed:cases.length-passed,total:cases.length,cases};console.log(JSON.stringify(report,null,2));if(passed!==cases.length)process.exitCode=1;
